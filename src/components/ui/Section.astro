---
// Standard section shell: 1120px content container, 24px side gutter,
// 120px/72px vertical padding (desktop/mobile). See design/NOTES.md.
interface Props {
  id: string;
  bg?: 'white' | 'gray-light' | 'charcoal';
  borderTop?: boolean;
  borderBottom?: boolean;
  /** Override the standard 72px/120px rhythm — e.g. Hero's asymmetric padding. */
  padding?: string;
  class?: string;
}

const {
  id,
  bg = 'white',
  borderTop = false,
  borderBottom = false,
  padding = 'py-section-mobile md:py-section',
  class: className = '',
} = Astro.props;

const bgClass = {
  white: 'bg-white',
  'gray-light': 'bg-gray-light',
  charcoal: 'bg-charcoal text-white',
}[bg];
---

<section
  id={id}
  class:list={[
    'px-6',
    padding,
    bgClass,
    borderTop && 'border-t border-border',
    borderBottom && 'border-b border-border',
    className,
  ]}
>
  <div class="mx-auto max-w-content">
    <slot />
  </div>
</section>
